import { ArraySearchPipePipe } from './array-search-pipe.pipe';

describe('ArraySearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
